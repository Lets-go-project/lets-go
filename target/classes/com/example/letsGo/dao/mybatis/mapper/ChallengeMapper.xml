<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.letsGo.dao.mybatis.mapper.ChallengeMapper">

  <cache />
    <select id="getAllRecords" resultType="String">
        select userId, r.swimDist, r.swimTime, u.gender
        from Record r join User u on r.userId = u.userId
    </select>
</mapper>