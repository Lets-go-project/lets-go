<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.letsgo.dao.mybatis.mapper.ProductMapper">

  <cache />

  <select id="getAllProducts" resultType="Product">
    SELECT
      PRODUCTID,
      PRODUCTNAME,
      PRODUCTTYPE as type
    FROM PRODUCT
    WHERE PRODUCTID = #{productId}
  </select>

  <select id="searchProductByProductType" resultType="Product">
    SELECT
      PRODUCTID,
      PRODUCTNAME,
      PRODUCTTYPE as type
    FROM PRODUCT
    WHERE PRODUCTTYPE = #{value}
  </select>

  <select id="searchProductByName" resultType="Product">
    select
      PRODUCTID,
      PRODUCTNAME as name,
      CATEGORY as type
    from PRODUCT
    WHERE lower(name) like #{value}
  </select>

</mapper>